# 时间规划 App：给 AI 的开发任务清单

## 怎么用这份文档

这份文档分三部分。第 0 节是**项目背景**，每次和 AI（Claude、Claude Code 或其他编程助手）开新对话时，先把这一段完整粘贴进去，让它知道你在做什么、用什么技术、有哪些限制。第 1 节定义 App 里 AI 需要理解的**"任务事件"**：数据长什么样、拆解规则是什么、输入输出格式是什么，它既是写代码的依据，也是之后给拆解 AI 的提示词来源。第 2 节是按顺序排好的**开发任务**，每次只把一个任务交给 AI，做完按验收标准自己检查，通过了再做下一个。

给 AI 下指令时可以用这个模板：

```
（先粘贴第 0 节项目背景）

【当前任务】
（粘贴第 2 节中的某一个任务，例如 T2.1 整段）

请只完成这个任务，不要提前做后面的功能。完成后：
1. 列出新建和修改的文件，给出完整文件内容；
2. 告诉我如何按"验收标准"一条条自测。
```

每完成一个任务就做一次 `git commit`，出了问题可以回退。遇到报错时，把完整报错原文贴回给 AI。

---

## 0. 项目背景（每次新对话先粘贴）

```
【项目背景】
我在做一个个人使用的时间规划 / 待办 App，只在我自己的 iPhone 上用。

核心流程：
我用文字，或 iPhone 键盘自带的语音听写，输入一件或几件要做的事
→ AI 把每件事整理成任务，并拆成可以马上开始做的小步骤
→ 我先预览、修改，确认后保存
→ 在列表里查看进度、勾选完成。

产品形式：PWA（网页应用），在 iPhone Safari 中"添加到主屏幕"后使用。

开发环境：Windows 电脑，没有 Mac。命令请给 Windows PowerShell 版本。

技术栈：
- 前端：React + TypeScript + Vite，样式用 Tailwind CSS
- PWA：vite-plugin-pwa
- 本地数据：IndexedDB，使用 Dexie
- AI 调用：经过 Cloudflare Worker 中转。Anthropic API key 只保存在 Worker 的 secret 中，
  前端代码和打包产物中绝对不能出现 key
- 部署：前端用 Cloudflare Pages，从 GitHub 自动部署

界面要求：简体中文，手机竖屏优先，适配 iPhone 刘海和底部安全区。

我的情况：（按实际情况改，例如"我学过一些编程，但不熟悉 React"）
请一次只做我给的那个任务，给出完整文件内容而不是片段，并说明每个文件放在哪里。
```

---

## 1. 核心定义：AI 要理解的"任务事件"

### 1.1 数据模型

放在 `src/types.ts`：

```ts
export type TaskStatus = 'todo' | 'doing' | 'done';
export type Priority = 'high' | 'medium' | 'low';

/** 一件要做的事 */
export interface Task {
  id: string;                 // uuid
  title: string;              // AI 整理后的简洁标题，如"完成 UX 课调研报告"
  rawInput: string;           // 我当时说的或写的原话，保留备查
  deadline: string | null;    // 截止日期 "YYYY-MM-DD"；没提到就是 null
  priority: Priority;
  status: TaskStatus;         // 有步骤被勾选 → doing；全部完成 → done
  steps: Step[];
  createdAt: string;          // ISO 时间
  updatedAt: string;
}

/** 任务下的一个小步骤 */
export interface Step {
  id: string;
  title: string;              // 动词开头的具体动作
  estimatedMinutes: number;   // 预估用时（分钟）
  done: boolean;
  doneAt: string | null;
  order: number;              // 排序，从 0 开始
}

/** 设置（存在本地） */
export interface Settings {
  maxMinutesPerStep: number;  // 每个步骤最长分钟数，默认 25，可选 15 / 25 / 45 / 60
  appPassword: string;        // 访问 Worker 的密码
}
```

### 1.2 拆解规则（之后直接改写成拆解 AI 的 system prompt）

1. **一句话可能包含多件事。** 例如"明天交报告，周末还得收拾房间"，要拆成两个独立任务。
2. **语音听写的文字不规范。** 可能有口头禅、同音错字、没有标点。先理解意图，再整理成干净的标题，不要照抄原话。
3. **步骤要"坐下就能开始做"。** 每个步骤以动词开头，内容具体；单个步骤的预估时间不超过 `maxMinutesPerStep`，超过就继续拆。
4. **步骤数量合理。** 每个任务 1 到 8 步。简单的事（如"给妈妈打电话"）只拆 1 步即可，不要硬拆。特别大的目标（如"学会 Python"），先拆出第一周能做的具体步骤，而不是列出整个学习计划。
5. **日期换算。** 根据请求中传入的"今天日期"和星期几，把"明天""这周末""下周五"换算成具体日期；这类说法有歧义时，按最常见的理解处理。没提到截止时间就填 `null`，不要编造。
6. **优先级。** 用户明确表达紧急或重要时填 `high`，明确说不急时填 `low`，其他情况一律 `medium`。
7. **信息太模糊时不要猜。** 例如"搞一下那个东西"，不返回任务，改为返回一个追问问题。
8. **只输出规定的 JSON 结构**，不输出任何多余文字。

### 1.3 接口协议

#### 接口一：`POST /decompose`，把输入拆成任务

请求（前端 → Worker），请求头带 `X-App-Password`：

```json
{
  "input": "下周五要交UX的调研报告 还没开始 然后这周末得把房间收拾了",
  "today": "2026-09-11",
  "weekday": "星期五",
  "maxMinutesPerStep": 25,
  "clarification": null
}
```

`clarification` 用于追问后的第二次请求，格式为 `{ "question": "AI 之前的追问", "answer": "我的回答" }`，第一次请求时为 `null`。

响应（Worker → 前端）：

```json
{
  "tasks": [
    {
      "title": "完成 UX 课调研报告",
      "deadline": "2026-09-18",
      "priority": "medium",
      "steps": [
        { "title": "重读作业要求，列出评分标准和字数要求", "estimatedMinutes": 15 },
        { "title": "确定调研问题，写出报告大纲", "estimatedMinutes": 25 },
        { "title": "查找并筛选 5 篇参考资料", "estimatedMinutes": 25 },
        { "title": "写引言和背景部分", "estimatedMinutes": 25 },
        { "title": "写调研发现部分初稿", "estimatedMinutes": 25 },
        { "title": "写结论与建议", "estimatedMinutes": 20 },
        { "title": "通读修改，检查引用格式后提交", "estimatedMinutes": 20 }
      ]
    },
    {
      "title": "收拾房间",
      "deadline": "2026-09-13",
      "priority": "medium",
      "steps": [
        { "title": "收起桌面和地上的杂物", "estimatedMinutes": 15 },
        { "title": "换床单，把脏衣服放进洗衣机", "estimatedMinutes": 15 },
        { "title": "扫地、拖地", "estimatedMinutes": 20 },
        { "title": "倒垃圾", "estimatedMinutes": 5 }
      ]
    }
  ],
  "question": null
}
```

信息太模糊时的响应：

```json
{
  "tasks": [],
  "question": "你说的“那个东西”具体是指什么？大概什么时候需要完成？"
}
```

#### 接口二：`POST /refine`，把某一个步骤拆得更细

请求：

```json
{
  "taskTitle": "完成 UX 课调研报告",
  "stepTitle": "写调研发现部分初稿",
  "maxMinutesPerStep": 15
}
```

响应：

```json
{
  "steps": [
    { "title": "整理访谈笔记，标出 3 个主要发现", "estimatedMinutes": 15 },
    { "title": "为每个发现写一段说明和证据", "estimatedMinutes": 15 },
    { "title": "补充一张总结表格", "estimatedMinutes": 10 }
  ]
}
```

#### 实现要求

Worker 调用 Claude API 时，用**强制工具调用（tool use）**的方式，让模型必须按上面的 JSON 结构输出，而不是靠提示词"请输出 JSON"。前端收到结果后，再用 zod 校验一次格式，不合格就当作失败处理。模型名放在 Worker 的环境变量 `MODEL` 中，方便更换；拆解这类任务可以先用速度快、价格低的模型（例如 Claude Haiku 系列），具体模型名以官方文档为准：https://docs.claude.com/en/api/overview

---

## 2. 开发任务（按顺序做）

总览：

|阶段|任务|完成后你能看到什么|
|-|-|-|
|M0 项目初始化|T0.1、T0.2|电脑和手机都能打开一个空页面|
|M1 数据层|T1.1 – T1.3|数据能存、能改、能备份|
|M2 界面（先不接 AI）|T2.1 – T2.4|可以手动管理任务的完整 App|
|M3 AI 中转后端|T3.1、T3.2|一个安全的 AI 接口|
|M4 接入 AI 拆解|T4.1 – T4.4|说一句话就能自动拆成步骤|
|M5 部署到 iPhone|T5.1、T5.2|装在主屏幕上日常使用|

---

### M0 项目初始化

#### T0.1 创建项目

**目标：** 搭好项目骨架，电脑和手机都能打开页面。

**要做：**

* 检查电脑是否已安装 Node.js（LTS 版本）和 Git，没有的话给出安装方法
* 用 Vite 创建 React + TypeScript 项目，项目名 `todo-planner`
* 安装并配置 Tailwind CSS
* 初始化 Git，推送到我的 GitHub 私有仓库

**验收标准：**

* `npm run dev` 后，电脑浏览器能打开页面
* 用 `npm run dev -- --host` 启动后，手机连接同一个 WiFi，用电脑的局域网 IP 能打开页面（打不开的话，排查 Windows 防火墙）
* GitHub 仓库里能看到代码

#### T0.2 PWA 基础配置

**目标：** 让网页具备"添加到主屏幕"后像 App 一样运行的能力。

**要做：**

* 安装配置 vite-plugin-pwa
* 配置 manifest：App 名称、图标（192px、512px）、`display: standalone`、主题色
* 添加 iOS 需要的 `apple-touch-icon` 和相关 meta 标签
* 页面布局适配 iPhone 刘海和底部安全区（`safe-area-inset`）
* 先生成一个简单的占位图标

**验收标准：**

* `npm run build` 成功，没有报错
* 电脑 Chrome 开发者工具 → Application → Manifest 面板没有报错
* （真机"添加到主屏幕"要等 T5 部署成 HTTPS 后再测）

---

### M1 数据层

#### T1.1 数据模型

**目标：** 把第 1.1 节的类型定义放进项目。

**要做：** 创建 `src/types.ts`，内容为第 1.1 节的代码。

**验收标准：** 项目编译没有类型报错。

#### T1.2 本地数据库

**目标：** 所有任务数据保存在手机本地，关掉 App 也不丢。

**要做：**

* 用 Dexie 建立 IndexedDB 数据库
* 实现这些函数：`listTasks`、`addTasks`（可一次添加多个）、`updateTask`、`deleteTask`、`addStep`、`updateStep`、`deleteStep`、`toggleStep`、`moveStep`（上移 / 下移）、`getSettings`、`saveSettings`
* 自动规则：有步骤被勾选时任务变为 `doing`，全部步骤完成时变为 `done`，取消勾选时相应回退
* 用 vitest + fake-indexeddb 为以上函数写单元测试

**验收标准：**

* 所有单元测试通过
* 在页面上临时加一个测试按钮写入数据，刷新页面后数据仍然存在

#### T1.3 导出与导入函数

**目标：** 防止 iPhone 清理数据导致任务丢失。

**要做：**

* `exportData()`：把所有任务和设置导出为 JSON 文件，文件名带日期
* `importData(file)`：读取 JSON，先校验格式，格式错误要给出提示
* 这一步只写函数，按钮在 T2.4 设置页里加

**验收标准：** 单元测试覆盖"导出 → 清空 → 导入"后数据与原来完全一致。

---

### M2 界面（先不接 AI）

#### T2.1 主页任务列表

**目标：** 一眼看到所有任务和进度。

**要做：**

* 分为"进行中"和"已完成"两组，"已完成"默认折叠
* 每个任务卡片显示：标题、截止日期（显示剩余天数，已过期显示红色）、进度条（已完成步骤数 / 总步骤数）、剩余预估时间
* 排序：有截止日期的按日期从近到远排在前面，没有截止日期的排在后面
* 点击卡片进入任务详情页
* 没有任务时显示空状态提示

**验收标准：**

* 用几条假数据展示正确
* 在 375px 到 430px 的手机宽度下文字不溢出、不挤压

#### T2.2 输入栏

**目标：** 方便地输入要做的事，支持键盘语音听写。

**要做：**

* 页面底部固定一个多行输入框和发送按钮
* 输入框字号不小于 16px（否则 iOS 聚焦时会自动放大页面）
* 占位提示："写下要做的事，或点键盘上的麦克风说话"
* 键盘弹出时，输入栏不能被键盘挡住
* 暂时的发送逻辑：把输入内容直接作为任务标题创建一个没有步骤的任务（T4.2 再替换成 AI 拆解）

**验收标准：**

* 在 iPhone 上通过局域网地址打开，用键盘听写能正常输入中文
* 键盘弹出时能看到输入框和发送按钮
* 发送后任务出现在列表中

#### T2.3 任务详情页

**目标：** 管理一个任务的所有步骤。

**要做：**

* 顶部显示标题、截止日期、优先级，点击可编辑
* 步骤列表：勾选完成（带简单动画）、点击编辑文字和预估时间、上移 / 下移按钮调整顺序、删除步骤
* 底部"添加步骤"按钮
* 显示总预估时间和剩余时间
* 删除整个任务（需要二次确认）
* 返回主页的按钮

**验收标准：** 以上所有操作在刷新页面后都能保留。

#### T2.4 设置页

**目标：** 集中管理设置和备份。

**要做：**

* 每步最长时间：15 / 25 / 45 / 60 分钟，默认 25
* App 密码输入框（用于访问 Worker）
* "导出备份"和"导入备份"按钮（调用 T1.3 的函数，导入前二次确认会覆盖现有数据）
* 主页右上角加进入设置页的入口

**验收标准：**

* 设置修改后刷新页面仍然保留
* 在 iPhone 上导出的文件可以保存到"文件" App

---

### M3 AI 中转后端

#### T3.1 Cloudflare Worker

**目标：** 一个安全的中转接口，前端通过它调用 AI，API key 永远不暴露。

**要做：**

* 在项目中新建 `worker/` 目录，用 Wrangler 创建 Worker
* 实现第 1.3 节定义的 `POST /decompose` 和 `POST /refine`
* 校验请求头 `X-App-Password` 是否等于 secret `APP_PASSWORD`，不一致返回 401
* 从 secret `ANTHROPIC_API_KEY` 读取 key 调用 Claude API，模型名读取环境变量 `MODEL`
* 把第 1.2 节的拆解规则写成 system prompt，用强制工具调用保证输出结构
* 请求中带上今天日期和星期几（由前端传入）
* CORS 只允许我的前端域名，本地开发时允许 localhost
* 输入内容超过 2000 字直接拒绝
* 出错时返回清晰的错误码和中文错误信息，但不要把 API 的原始报错细节透露给前端

**验收标准：**

* 本地用 `wrangler dev` 启动后，在 PowerShell 中用 `curl.exe` 测试：密码错误返回 401；密码正确时返回符合第 1.3 节格式的 JSON
* 第 1.3 节的示例输入能得到合理的拆解结果

#### T3.2 部署 Worker

**目标：** 把 Worker 部署到线上。

**要做：**

* 用 `wrangler secret put` 设置 `ANTHROPIC_API_KEY` 和 `APP_PASSWORD`（我自己在终端里输入，不要把 key 写进任何文件或提交到 Git）
* 确认 `.gitignore` 排除了 `.dev.vars` 等本地密钥文件
* `wrangler deploy` 部署，记下 Worker 地址

**验收标准：**

* 线上 Worker 用 `curl.exe` 测试结果与本地一致
* 在 GitHub 仓库中全局搜索 `sk-ant`，搜不到任何结果

---

### M4 接入 AI 拆解

#### T4.1 拆解效果测试（最关键的一步）

**目标：** 在写界面之前，先确认 AI 拆得好不好用。

**要做：**

* 准备 10 条测试输入，覆盖这些情况：单件事；一句话多件事；口语化、带口头禅、没标点；有相对日期（明天、这周末、下周五）；没有日期；很简单的事；很模糊的事；很大的目标（如"学会 Python"）；中英文混合；带明确"很急"的事
* 写一个脚本，批量调用线上 Worker，把输入和结果整理输出到 `test-results.md`

**验收标准：**

* 我逐条人工检查过，对拆解结果满意
* 不满意的地方修改 system prompt 后重新跑，直到满意为止

#### T4.2 前端接入拆解

**目标：** 输入一句话，自动生成任务和步骤。

**要做：**

* 替换 T2.2 的临时发送逻辑：调用 `/decompose`，Worker 地址从环境变量 `VITE_WORKER_URL` 读取，密码从设置读取
* 等待时显示加载状态，禁止重复发送
* 结果先进入"预览确认"界面：可以修改标题、日期、步骤，删除不需要的任务或步骤，确认后才保存
* 用 zod 校验返回格式
* 失败处理：没有网络、超过 20 秒超时、密码错误（提示去设置页检查）、返回格式不对。每种情况都给出中文提示，并提供"重试"和"不拆解，直接保存为任务"两个选项
* 保存时记录 `rawInput` 原话

**验收标准：**

* 正常网络下，输入一句话能拆解、预览、修改并保存
* 开启飞行模式发送时不会卡住，有明确提示，并能选择直接保存

#### T4.3 追问

**目标：** 信息不够时 AI 会问我，而不是乱猜。

**要做：**

* 返回结果中有 `question` 时，在预览界面显示这个问题和一个回答输入框
* 我回答后，把原始输入、问题和回答（`clarification` 字段）一起重新调用 `/decompose`

**验收标准：** 输入"搞一下那个事情"会被追问，回答后能正确拆解。

#### T4.4 单个步骤再拆细

**目标：** 某一步还是太大时，一键拆得更细。

**要做：**

* 详情页每个未完成步骤旁边加"拆细"按钮
* 调用 `/refine`，用返回的几个小步骤替换原步骤，保持在原来的位置
* 替换前显示预览，可以取消

**验收标准：** 把"写调研发现部分初稿"拆细后，变成 2 到 4 个步骤，并且位置正确。

---

### M5 
部署到 iPhone

#### T5.1 部署前端

**目标：** 前端上线，获得 HTTPS 网址。

**要做：**

* 在 Cloudflare Pages 连接 GitHub 仓库，构建命令 `npm run build`，输出目录 `dist`
* 配置环境变量 `VITE_WORKER_URL`
* 更新 Worker 的 CORS 设置，允许 Pages 的域名

**验收标准：**

* 电脑访问 Pages 网址，所有功能正常
* 推送代码到 GitHub 后会自动重新部署

#### T5.2 iPhone 真机验收

按下面清单逐条检查：

* [ ] Safari 打开网址 → 分享 → 添加到主屏幕，图标和名称正确
* [ ] 从主屏幕打开是全屏显示，没有 Safari 地址栏
* [ ] 顶部刘海和底部横条区域没有遮挡内容
* [ ] 在设置页填写密码后，用键盘听写说一句话，能拆解、预览并保存
* [ ] 从后台彻底关掉 App 再打开，数据还在
* [ ] 飞行模式下能打开 App，查看和勾选已有任务
* [ ] 导出备份文件能存到"文件" App

---

## 3. 以后再做（现在先不要交给 AI）

* **推送提醒：** 截止日期前或每天固定时间提醒。iOS 主屏幕网页应用支持推送，但需要 Worker 定时任务和推送密钥配置。
* **按住说话：** App 内录音，发给语音转文字服务，不再依赖键盘听写。
* **今日计划：** AI 根据各任务的截止日期和剩余步骤，推荐今天做哪几步。
* **统计：** 每周完成步骤数，预估用时和实际用时的对比。
* **升级原生 App：** 用 Expo 重写界面，复用数据模型和拆解逻辑，配合苹果开发者账号打包。



## UI设计

针对 ADHD 与神经多样性（Neurodivergent）人群的时间规划类产品（如 **[Tiimo](https://www.tiimoapp.com/)**、**Structured**、**Llama Life**），核心设计目标不是“把所有日程堆在一起”，而是**降低认知负荷（Cognitive Load）、对抗时间盲（Time Blindness），并提供温和的多巴胺正向反馈**。



以下从**前端技术栈/UI框架建议**与**ADHD 核心视觉/交互设计规范**两个维度为你梳理：



---



### 一、 前端高颜值 UI 框架与库建议



要在移动端（PWA / 响应式 Web）做出类似 Tiimo 这种柔和、温润、具象化的视觉风格，建议采用 **Tailwind 生态 + Headless 无样式组件 + 物理弹簧动效** 的组合：



1. **组件库推荐**

* **[shadcn/ui](https://ui.shadcn.com/)**（配合 Radix Primitives）：

* **优势**：代码直接复制进项目，样式 100% 自由控制，天然具备极高的无障碍属性（A11y），可以通过全局 CSS 变量快速调配出 Tiimo 风格的超大圆角与柔和配色。





* **[Ark UI](https://ark-ui.com/)**（Chakra 团队出品）：

* **优势**：状态机驱动，非常适合移动端的多状态切换（如手势滑动、轻弹通知、多步骤折叠展开）。









2. **动效库（ADHD 设计的灵魂）**

* **[Framer Motion](https://motion.dev/)**：

* **为什么必备**：Tiimo 这类 App 极度依赖“物理直觉”。通过弹簧阻尼动画（Spring Physics）让步骤完成时的划掉、卡片拖拽、展开收起具有“弹性质感”，能给予大脑清晰的因果反馈。









3. **图标与多感官视觉库**

* **[Phosphor Icons](https://phosphoricons.com/)**（推荐 **Duotone** 双色调模式）：

* **为什么推荐**：相比常规扁平单色图标，Duotone 带有柔和的半透明背景填充，圆润温和，视觉传达直观，能有效减轻 ADHD 人群的文字阅读疲劳。





* **[Lucide Icons](https://lucide.dev/)**：现代精炼，圆角平滑。





4. **手势与拖拽排序**

* **[@dnd-kit](https://dndkit.com/)**：针对移动端触控优化的拖拽库，支持长按拖拽步骤重排，在手机浏览器中操作非常跟手。







---



### 二、 借鉴 Tiimo 的 5 大 ADHD 核心设计原则



#### 1. 时间具象化：用几何图形对抗“时间盲（Time Blindness）”



* **避免纯数字倒计时**：跳动的 `23:45` 数字倒计时容易让人产生压迫感与焦虑；

* **视觉化饼图/弧线进度（Visual Arc/Circle）**：像 Tiimo 那样使用**环形进度条（Donut/Pie Timer）**或**胶囊进度条**。当时间过去，色块逐渐被清空或填充，让时间变成有体积、看得见的实体。



#### 2. “Now / Next” 聚焦机制：抑制执行功能障碍（Executive Dysfunction）



* **单步聚焦模式（Spotlight View）**：

* 满屏的待办清单会导致大脑死机（Task Paralysis）；

* 主屏最显眼的位置只放“当前正在做的那一步”**，并用小字附带**“紧接着的下一步”；

* 剩余的步骤默认收纳在抽屉或折叠面板中，想看时再展开。







#### 3. 双重编码（Dual Coding）：图标 + 文字，弱化认知解析



* **图标先行**：每个任务/步骤前面搭配一个代表动作的直观图标（如：书籍 📖 代表阅读、放大镜 🔍 代表调研、扫把 🧹 代表清洁）；

* **动词开头的精炼语言**：步骤文本尽量在 10~15 字以内，并且必须以具体动作动词开头（“打开电脑查 3 篇资料”而不是“资料搜集阶段”）。



#### 4. 低感官刺激色彩系统（Sensory-Friendly Palette）



* **拒绝刺眼的警报色**：ADHD 用户对红色警示符号容易产生挫败感和逃避心理。不要用大面积的高饱和警报红；

* **莫兰迪/柔和粉彩（Pastel Tones）分类**：

* 学习/工作：薄雾灰蓝、鼠尾草绿；

* 生活/日常：淡奶油杏色、暖粉紫；

* 界面背景：避免纯白（`#FFFFFF`）与纯黑（`#000000`）的高反差，推荐暖白/纸感浅灰（如 `#FBFBFA`、`#F5F5F3`）搭配柔和卡片背景。




#### 5. 宽容型设计与多巴胺微激励（Forgiving & Rewarding）



* **微小的正反馈（Dopamine Boost）**：

* 点击步骤完成时，触发一次流畅的划线微弹跳动效，并调用移动端震动 API（`navigator.vibrate`）提供轻触感反馈；





* **不批评机制（No-Guilt Scheduling）**：

* 任务如果超过预定截止时间，界面不要显示“你已逾期”这种带有指责意味的红色警告；

* 提供“延后 15 分钟”、“明天再做”或“重新切碎步骤”的单键快捷操作。







---



### 三、 界面布局与视觉规范建议



| 设计要素 | 建议规范 | 预期效果 |

| --- | --- | --- |

| **卡片圆角** | `rounded-2xl` 至 `rounded-3xl`（16px ~ 24px） | 消除尖锐棱角，增强亲和力与安全感 |

| **阴影风格** | 超浅扩散漫反射阴影（Soft Ambient Shadow），无生硬硬边 | 卡片浮于纸面感，层次分明但不杂乱 |

| **字号层级** | 标题 20~24px（Semibold），步骤正文 16px，补充标签 13~14px | 层次清晰，同时保证移动端不触发 Safari 自动缩放 |

| **点击目标** | 所有按钮与勾选框热区不小于 44×44pt | 容错率高，单手点击轻松 |

进阶玩法：
在顶部设计“日期切换栏（Date Strip）”是时间规划类 App 中非常经典的交互，但针对 **ADHD 和时间管理场景**，这个顶部区域还可以做得更具**视觉暗示、降低焦虑并辅助决策**。

以下是类似且非常有价值的几种**顶部时间导航设计与衍生模式**：

---

### 一、 顶部日期栏本身的“进阶玩法”（借鉴 Tiimo / Structured / Things）

#### 1. 任务负荷圆点 / 迷你完成环（Load Indicators & Progress Rings）

* **做法**：顶部横滑的日期胶囊（如 `周五 11`、`周六 12`）下方，不只显示数字，而是带上**微型视觉标记**：
* **彩色小圆点（1~3个）**：代表当天的任务密度（浅点=轻松，3个点=当天排满了）；
* **微型圆环进度条（Tiimo 风格）**：随着当天任务被勾选，日期下方的环形逐渐填满。


* **为什么适合 ADHD**：直观解决过度承诺（Over-committing）的问题——准备往某天加任务时，扫一眼顶部日期栏就能看出来哪天已经很拥挤，避免把事情堆死在某一天。

#### 2. 一键“回到今天”（Today Quick-Snap）

* **做法**：当你手势横滑查看后天、下周时，顶部或右上角自动浮现一个圆润的 `回到今天` 快捷药丸按钮；点击后通过弹簧动效（Spring Animation）瞬间吸附回到当天。
* **效果**：浏览未来不迷路，永远给用户一个安全、即时归位的抓手。

#### 3. 相对时间视角（Relative Horizon：今天 / 明天 / 本周末）

* **做法**：ADHD 大脑对冷冰冰的日历数字（比如“9月18日”）往往没有距离感，容易产生“还很远”的错觉。可以在顶部日期栏或快捷切换器中，加入**口语化、相对性的标签**：
* `今天` · `明天` · `这周末` · `下周`


* **效果**：将模糊的数字日期转化为直观的心理倒计时，降低大脑换算时间的时间差阻力。

---

### 二、 类似的“时间与任务维度”导航设计

除了单日切换，还有以下几种与日期类似但更灵活的时间组织功能：

#### 4. “3日滚动视界”（3-Day Horizon View）

* **痛点**：ADHD 人群往往只有两种时间概念——**“现在（Now）”** 和 **“不是现在（Not Now）”**。如果只看今天，很容易忽视明天的紧迫任务；如果看一整月，信息又太杂乱。
* **做法**：
* 顶部支持一键切到“3日视图”（昨天 · 今天 · 明天）；
* 让你既能一眼看见明天有什么不可推迟的事，又不会被太长远的未来吓退。



#### 5. 分时段区块切换（Time-of-Day Blocks: 晨间 / 午后 / 晚间）

* **做法**：不把任务绑定到精确的“几点几分”（一旦某分钟没开始容易引发全盘放弃的放弃心态），而是将每天在顶部划分为 3~4 个柔和的时间区块：
* 🌅 **晨间启动**（早晨例行、简单准备）
* ☀️ **午后攻坚**（主任务、调研、写报告）
* 🌙 **晚间收尾**（收拾、复盘、轻任务）


* **效果**：在松弛感与结构化之间找到平衡，既有时间锚点，又不会因为迟到了 10 分钟产生负罪感。

#### 6. “无日期 / 某天”收纳仓（Someday / Anytime Pocket）

* **做法**：在顶部横滑日期条的最左端，放置一个特殊的图标标签（例如一个储物盒 📦 或无限符号 `∞`），代表“未定日期（Someday）”。
* **效果**：很多临时记录的想法不需要硬塞给某一天。放在这里，当某天你提前做完了事情、或者精力充沛时，随时点开拖进今天的列表。

#### 7. “昨日未结”晨间温和回顾（Morning Rollover）

* **做法**：每天早晨第一次打开 App 时，顶部温和弹出一张小卡片：“昨天有 2 个步骤没来得及完成，今天想顺延处理吗？”
* 提供两个大圆角按钮：【一键移到今天】 或 【移入杂物箱】。


* **效果**：不让没做完的事悄无声息地变成“红色逾期警告”，而是当成全新的起点，帮助用户无压力开启新的一天。


